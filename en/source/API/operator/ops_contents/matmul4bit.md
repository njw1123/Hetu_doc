# hetu.matmul4bit

```python
hetu.matmul4bit(a: hetu.tensor, b: hetu.tensor, absmax: hetu.tensor, datatype: hetu.tensor, trans_a: bool = False, trans_b: bool = False, blocksize: int = 4096) -> hetu.tensor
```

